// Übertrage folgende Rechenarten in deinen Code - Editor und schaue dir das Ergebnis an:

// Verwende Grundrechenarten
// Addition
let additionOperator = 1 + 1;
console.log("Addition: " + additionOperator);

// Subtraktion
let subtraktionOperator = 2 - 1;
console.log("Subtraktion: " + subtraktionOperator);

// Multiplikation
let multiplicationOperator = 2 * 2;
console.log("Multiplikation: " + multiplicationOperator);

// Division
let divisionOperator = 4 / 2;
console.log("Division: " + divisionOperator);

// Modulo: zeigt an, was der Rest sein würde.
let moduloOperator = 14 % 5;
console.log("Modulo: " + moduloOperator);
